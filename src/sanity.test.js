import { describe, expect, it } from 'vitest';

// Placeholder so `npm test` exercises the runner. Delete once real tests exist.
describe('test runner', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
